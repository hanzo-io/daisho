input(id='{ input.name }',name='{ name || input.name }',type='checkbox',onchange='{ change }',onblur='{ change }',checked='{ input.ref.get(input.name) }',class='{invalid: errorMessage, valid: valid}')
label.label(for='{ name || input.name }') { label }
#{ 'yield' }

