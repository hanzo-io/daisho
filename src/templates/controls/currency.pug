input(id="{ input.name }",name="{ name || input.name }",type="{ type }",onchange="{ change }",riot-value="{ currencyValue() }",autocomplete="{ autoComplete }",class="{invalid: errorMessage, valid: valid}")
.placeholder(class='{ small: currencyValue() }')
  | { placeholder }
#{ 'yield' }
