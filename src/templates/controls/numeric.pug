input(id="{ input.name }",name="{ name || input.name }",type="{ type }",onchange="{ change }",riot-value="{ numericValue() }",autocomplete="{ autoComplete }",class="{invalid: errorMessage, valid: valid}")
.placeholder(class='{ small: numericValue() || numericValue() == 0 }')
  | { placeholder }
#{ 'yield' }

