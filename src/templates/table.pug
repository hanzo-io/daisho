.table-header
  .table-cell(each='{ h, k in tableColumns }') { h }
daisho-table-row.table-row(each='{ item, k in data.get(tableField) }', data='{ parent.data.ref(parent.tableField ? parent.tableField + "." + k : k) }')
  #{ 'yield' }
